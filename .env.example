# ─────────────────────────────────────────────────────────────
# Noviq — environment variables (copy to .env, never commit .env)
# ─────────────────────────────────────────────────────────────

# --- Chain (HSK Chain testnet, chainId 133) ---
HSK_RPC_URL="https://testnet.hsk.xyz"
NEXT_PUBLIC_HSK_RPC_URL="https://testnet.hsk.xyz"
# testnet explorer (explorer.hsk.xyz is mainnet)
NEXT_PUBLIC_HSK_EXPLORER_URL="https://testnet-explorer.hsk.xyz"
NEXT_PUBLIC_HSK_CHAIN_ID="133"

# --- AI (Gemini via OpenRouter) ---
# https://openrouter.ai/keys
OPENROUTER_API_KEY=""

# --- Persistence (Postgres — Neon/Supabase) ---
# Pooled endpoint for the app/worker runtime.
DATABASE_URL=""
# Direct (non-pooled) endpoint for drizzle-kit migrations/push (no "-pooler" host).
DATABASE_URL_UNPOOLED=""

# --- Agent session key (relayer holds ONLY this scoped key) ---
# Throwaway testnet key funded with a little HSK for gas. NEVER a mainnet key.
AGENT_PRIVATE_KEY=""
AGENT_ADDRESS=""

# --- Deployer / agent keys are stored in contracts/.env (gitignored),
#     NOT here. See contracts/.env.example.
